/**
 * Імена кадрів атласа — єдиний список на гру, генератор і тести.
 *
 * Чому списком, а не рядками на місцях. Найдешевша й найпідступніша помилка
 * атласа — одруківка в імені: спрайт просто не малюється, помилки немає,
 * і бачиш це вже на телефоні. Тут ім'я — тип, тож `p.at('anchr', …)` не
 * компілюється.
 *
 * Перша спроба перевіряти це тестом (пошук імен регуляркою по коду) впала
 * при першому ж рефакторингу: імена почали передаватися через проміжну
 * функцію, і тест оголосив живі кадри мертвими. Тип цього не вміє.
 */
export const FRAMES = [
  'hero', 'anchor', 'glow', 'px', 'rope', 'spark', 'ground', 'wall', 'bgFar', 'bgNear',
] as const;

export type FrameName = typeof FRAMES[number];
