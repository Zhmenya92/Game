# Звідки стартувати

**Оновлено:** 2026-08-31, кінець дня
**Гілка:** `claude/mobile-game-minimal-animation-n0frv7` · останній комміт `7c7610f`
**Дашборд:** https://claude.ai/code/artifact/65bb21d4-a4a0-4874-87da-c98339b74f0a

Цей файл існує з однією метою: наступного разу не витрачати годину на «а що вже зроблено». Детальний хід — `game/NOTES.md`, обґрунтування рішень — `plan.md`.

---

## 1. Де ми зараз, одним абзацом

Побудовано **грабельний прототип концепту К4 «Павутина»**: чужі пробіжки стають фізичними лініями рівня, за які чіпляються гаком. Він грається на телефоні, має бекенд із серверною верифікацією рахунку, віральну петлю з викликами через Telegram-діп-лінки й арт-пайплайн з атласом. Пройдено п'ять тижнів плану з дев'яти. **97 автоматичних перевірок + 1000 сідів на прохідність, усе зелене.**

| Тиждень | Що | Стан |
|---|---|---|
| 1 | Детермінований кор, генерація траси польотом, тести | ✅ |
| 2 | Джус і **рій невдач** (усі спроби програються одночасно) | ✅ |
| 3 | Бекенд, `initData`, верифікація рахунку переграванням, чужа павутина | ✅ |
| 4 | Виклики через `?startapp=`, метрики гейта 3 | ✅ |
| 5 | Арт-пайплайн, атлас 512×512 / 14.2 КБ, рендер однією текстурою | ✅ |
| 6 | **Монетизація + аналітика** — Stars, Adsgram, дашборд | ⏭ **наступний** |
| 7–8 | Софтлонч, збір D1/D7/K на реальній аудиторії | 🚦 гейт 4 |
| 9+ | Стор-фаза: Capacitor → Play closed testing → App Store | — |

---

## 2. Як це запустити

```
cd game
npm install          # якщо ще не ставили
npm run atlas        # згенерувати public/atlas.png + atlas.json
npm run build        # атлас + vite build
npm test             # усі шість наборів: детермінізм, реплей, сервер, виклики, арт, прохідність

# три сервіси
DEV_ALLOW_UNSIGNED=1 node server/index.ts        # API, порт 8790
npx vite preview --port 8788 --host 0.0.0.0      # гра, порт 8788
node ../serve-dashboard.mjs                      # дашборд, порт 8787
```

З телефона в тій самій мережі: `http://<ip машини>:8788`.

Корисне окремо:
- `npm run preview` — **знімок кадру гри в PNG без браузера** (`node tools/preview.ts [сід] [кадр] [файл]`). Найшвидший спосіб побачити, що зламав рендер.
- `node tools/trace.ts` — покадровий друк позиції, швидкості й стану кнопки. Тричі рятував там, де перебір параметрів не давав нічого.

---

## 3. Що блокує і чекає на власника

Це не «список побажань» — без цих чотирьох пунктів проєкт не рухається далі гейта 3.

| # | Що потрібно | Що це розблокує |
|---|---|---|
| 1 | **Токен від BotFather** (`BOT_TOKEN`, `BOT_NAME`, `APP_NAME`) | Справжній шеринг через `savePreparedInlineMessage` — зараз кнопка йде через Web Share API, тобто системне меню «Поділитися», а не інлайн-повідомлення в чат. Формат посилання вже правильний, заміна — в одному місці. Плюс справжня валідація `initData` замість `DEV_ALLOW_UNSIGNED=1` |
| 2 | **50–100 живих гравців** (свої чати, тематичні спільноти) | 🚦 **Гейт 3** — найважливіший гейт плану. `/api/metrics` рахує все правильно, але рахувати поки нема що |
| 3 | **Spike 0.2 — формат кліпу** (GIF / MP4 / PNG-картка) | Артефакт, що летить у чат. Єдиний незакритий пункт розвідки з 30.08 |
| 4 | **Гра на реальному телефоні, руками** | Час до першого кадру, відчуття керування на дотик, кількість викликів малювання. Знімки кадру я перевірив — рух і тактильність перевірити з коду неможливо |

---

## 4. Звідки стартувати завтра

**Рекомендація: тиждень 6, але з двох частин брати спершу аналітику, а не гроші.**

Причина: `plan.md` 10.1 перелічує 13 подій, без яких «гейти неможливо порахувати». Ми зараз шлемо **п'ять**:

| Є | Немає |
|---|---|
| `app_open`, `run_end`, `share_click`, `challenge_opened`, `challenge_replied` | `run_start`, `share_sent`, `ghost_beaten`, `day_streak`, `ad_offer`, `ad_watched`, `iap_open`, `iap_purchased` |

Перші чотири з відсутніх пишуться сьогодні й не потребують нічого зовнішнього. **`ghost_beaten` — найцінніша:** це подія «обійшов конкретного друга», тобто прямий вимір того, чи працює диференціація. Її немає взагалі.

Далі — монетизація. Тут одразу два застереження з плану, які варто перечитати перед роботою:
- **Stars:** мінімум виводу **1 000 Stars**, холд **21 день**, ефективно **~$0.009/Star** при мобільній купівлі. Тобто «швидко перевірити монетизацію» не вийде за визначенням. Обов'язковий `/paysupport` — без нього порушення правил.
- **Adsgram:** eCPM ⚠️ джерела розходяться в 15 разів ($0.40–1.50 проти $5–6). Планувати за нижнім, міряти емпірично.

**Альтернатива, якщо хочеться швидшого ефекту:** не тиждень 6, а полірування того, що вже є, під живий запуск для гейта 3 — бо гейт 3 усе одно попереду монетизації, і саме він вирішує долю проєкту. Тоді порядок такий: токен бота → справжній шеринг → 50 людей → метрики. Монетизація без гейта 3 — це гроші з механіки, про яку ще не доведено, що вона розходиться.

---

## 5. Відкриті рішення (`plan.md`, розділ 17)

| # | Питання | Стан |
|---|---|---|
| 2 | Юрособа для Google Play | не вирішено; економить 2 тижні закритого тестування |
| 3 | Хостинг бекенду | не вирішено; рекомендація — Cloudflare Workers. Зараз усе в пам'яті процесу, інтерфейси `RunStore` і `ChallengeStore` вузькі навмисно |
| 5 | Формат кліпу | не вирішувати до spike 0.2 |
| 7 | **Тема й сеттинг** | ⚠️ план каже «ухвалити **до тижня 5**» — тиждень 5 пройдено, рішення немає. Зараз тема нейтральна: темне небо, силуети, павутина. Коштує 0 днів коду, але чим пізніше, тим більше кадрів переробляти |
| 8 | PlayDeck як канал | оцінити після гейта 3 |

---

## 6. Чого ми **не** довели

Список навмисно тут, а не в кінці якогось довгого файлу.

- **Гейт 3 не пройдено.** Уся диференціація перевірена лише ботами й мною.
- **Час до першого кадру ≤2 с** — не виміряно, потрібен пристрій і 4G.
- **1–2 виклики малювання на сцену** — не виміряно, потрібен профайлер GPU. Перевірено лише передумову: усе ігрове поле йде одним атласом. Текст HUD — чотири об'єкти `Text` із власними текстурами, тож без растрового шрифта в атласі гейт недосяжний.
- **Арт — заготовки, намальовані кодом.** Кроки [1]–[5] пайплайну плану (генерація, витяг альфи, ручне вирівнювання pivot) не виконані. Справжній арт кладеться в `game/art/src/<ім'я кадру>.png` — код не змінюється.
- **Крос-платформний детермінізм** (iOS проти Android) перевірявся лише всередині одного рушія. Тест ловить недетермінізм у коді, а не різницю платформ.
- **Ретеншен D1/D7** — жодного числа, і не буде до софтлончу.

---

## 7. Урок, який повторився тричі

Тижні 3, 4 і 5 дали ту саму знахідку: **зелений тест доводить лише те, що він перевіряє.**

- Тиждень 3: «зелений» тест на підміну павутини був порожнім — і саме він **ховав** дефект, що павутина декоративна.
- Тиждень 4: чотири дефекти, що підробляли метрики гейта, знайдені **читанням коду після** того, як усі набори стали зелені.
- Тиждень 5: рендер не перевіряв жоден тест взагалі — довелося винести розкладку кадру з Phaser і навчитися малювати її в PNG, щоб просто **побачити**.

Практичний висновок на завтра: після кожної зеленої збірки питати не «чи проходять тести», а «що саме вони не питають».
