# Звідки стартувати

**Оновлено:** 2026-08-31, кінець дня
**Гілка:** `claude/mobile-game-minimal-animation-n0frv7` · останній комміт `6d84e8a`
**Дашборд:** https://claude.ai/code/artifact/65bb21d4-a4a0-4874-87da-c98339b74f0a

Цей файл існує з однією метою: наступного разу не витрачати годину на «а що вже зроблено». Детальний хід — `game/NOTES.md`, обґрунтування рішень — `plan.md`, як це перевіряти — `testing-plan.md`.

---

## 1. Де ми зараз, одним абзацом

Побудовано **грабельний прототип концепту К4 «Павутина»**: чужі пробіжки стають фізичними лініями рівня, за які чіпляються гаком. Він грається на телефоні, має бекенд із серверною верифікацією рахунку, віральну петлю з викликами через Telegram-діп-лінки, арт-пайплайн з атласом і монетизацію з аналітикою. Пройдено **шість тижнів плану з дев'яти**. **172 автоматичні перевірки + 1000 сідів на прохідність, усе зелене.**

| Тиждень | Що | Стан |
|---|---|---|
| 1 | Детермінований кор, генерація траси польотом, тести | ✅ |
| 2 | Джус і **рій невдач** (усі спроби програються одночасно) | ✅ |
| 3 | Бекенд, `initData`, верифікація рахунку переграванням, чужа павутина | ✅ |
| 4 | Виклики через `?startapp=`, метрики гейта 3 | ✅ |
| 5 | Арт-пайплайн, атлас 512×512 / 14.2 КБ, рендер однією текстурою | ✅ |
| 6 | Монетизація й аналітика — Stars, Adsgram, власний дашборд | ✅ |
| — | План тестування — `testing-plan.md` | ✅ |
| 7–8 | Софтлонч, збір D1/D7/K на реальній аудиторії | 🚦 гейт 4 |
| 9+ | Стор-фаза: Capacitor → Play closed testing → App Store | — |

---

## 2. Як це запустити

```
cd game
npm install          # якщо ще не ставили
npm run atlas        # згенерувати public/atlas.png + atlas.json
npm run build        # атлас + vite build
npm test             # сім наборів: детермінізм, реплей, сервер, виклики,
                     # монетизація, арт, прохідність

# три сервіси
DEV_ALLOW_UNSIGNED=1 ADSGRAM_SECRET=dev-ad-secret \
  TELEGRAM_WEBHOOK_SECRET=dev-hook-secret node server/index.ts   # API :8790
npx vite preview --port 8788 --host 0.0.0.0                      # гра :8788
node ../serve-dashboard.mjs                                      # план :8787
```

З телефона в тій самій мережі: `http://<ip машини>:8788`.
Аналітика в реальному часі: `http://<ip машини>:8790/dashboard`.

Корисне окремо:
- `npm run preview` — **знімок кадру гри в PNG без браузера** (`node tools/preview.ts [сід] [кадр] [файл]`). Найшвидший спосіб побачити, що зламав рендер.
- `node tools/trace.ts` — покадровий друк позиції, швидкості й стану кнопки. Тричі рятував там, де перебір параметрів не давав нічого.

---

## 3. Що блокує і чекає на власника

Це не «список побажань» — без цих чотирьох пунктів проєкт не рухається далі гейта 3.

| # | Що потрібно | Що це розблокує |
|---|---|---|
| 1 | **Токен від BotFather** (`BOT_TOKEN`, `BOT_NAME`, `APP_NAME`, `TELEGRAM_WEBHOOK_SECRET`) | Справжній шеринг у чат, справжню валідацію `initData` замість `DEV_ALLOW_UNSIGNED=1` і **живі оплати Stars**. Увесь код оплат написаний і перевірений з нашого боку, але проти живого Telegram не працював жодного разу |
| 2 | **50–100 живих гравців** (свої чати, тематичні спільноти) | 🚦 **Гейт 3** — найважливіший гейт плану. `/dashboard` рахує все правильно, але рахувати поки нема що |
| 3 | **Акаунт Adsgram і block id** (`VITE_ADSGRAM_BLOCK`, `ADSGRAM_SECRET`) | Rewarded-рекламу за продовження. Без block id кнопка реклами просто не показується |
| 4 | **Spike 0.2 — формат кліпу** (GIF / MP4 / PNG-картка) | Артефакт, що летить у чат. Єдиний незакритий пункт розвідки з 30.08 |
| 5 | **Гра руками на телефоні** | Час до першого кадру, відчуття керування на дотик, кількість викликів малювання. Знімки кадру перевірені — рух і тактильність з коду не перевіряються |

---

## 4. Звідки стартувати завтра

**Рекомендація: не тиждень 7, а `testing-plan.md`, етапи 0–4.**

Причина: тижні 7–8 плану — це софтлонч і гейт 4, тобто те, що потребує людей і токена. А `testing-plan.md` містить **тиждень роботи, яка не залежить ні від кого**: евристична самоперевірка, 44 ручні кейси на пристроях, виправлення, два кола плейтесту. Це найдешевший спосіб не привести п'ятдесят людей на гру, у якій щось очевидно зламано.

**Три речі, які план тестування знайшов ще до першого ручного кейса:**

1. **У грі немає жодного налаштування** — ні складності, ні звуку (звуку немає взагалі), ні контрасту. Порушує і евристику Pinelle, і базовий рівень Game Accessibility Guidelines.
2. **Чужі лінії відрізняються від власних лише кольором** — бірюзовий проти помаранчевого. Це саме та пара, яку плутають при дейтеранопії. Потрібна різниця у товщині або пунктирі.
3. **Підказка на старті зникає за таймером** (150 кадрів), а не за дією гравця. Базовий рівень доступності вимагає темпу читання за гравцем.

Усі три — робота на години, і всі три виявляться на плейтесті, якщо їх не закрити.

**Якщо все ж хочеться далі за планом:** тиждень 7 — софтлонч. Порядок такий: токен бота → живі оплати перевірити на собі → 50 людей → гейт 3 → тільки потім D1/D7.

---

## 5. Відкриті рішення (`plan.md`, розділ 17)

| # | Питання | Стан |
|---|---|---|
| 2 | Юрособа для Google Play | не вирішено; економить 2 тижні закритого тестування |
| 3 | Хостинг бекенду | не вирішено; рекомендація — Cloudflare Workers. Зараз усе в пам'яті процесу, інтерфейси `RunStore`, `ChallengeStore` і `Wallet` вузькі навмисно |
| 5 | Формат кліпу | не вирішувати до spike 0.2 |
| 7 | **Тема й сеттинг** | ⚠️ план каже «ухвалити **до тижня 5**» — тиждень 5 пройдено, рішення немає. Зараз тема нейтральна: темне небо, силуети, павутина. Коштує 0 днів коду, але чим пізніше, тим більше кадрів переробляти |
| 8 | PlayDeck як канал | оцінити після гейта 3 |
| — | **Ціни в Stars** | 25 ⭐ за 3 продовження, 75 ⭐ за 10, 60 ⭐ за скін — це START-значення, не результат вимірювання |

---

## 6. Чого ми **не** довели

Список навмисно тут, а не в кінці якогось довгого файлу. Повна версія — `testing-plan.md`, розділ 4.

- **Гейт 3 не пройдено.** Уся диференціація перевірена лише ботами й мною.
- **Живих оплат не було жодної.** Перевірено нашу половину: розбір оновлень, ідемпотентність, відмови. Telegram цього коду ще не бачив.
- **Живої реклами не було жодної.** Потрібен акаунт Adsgram.
- **Час до першого кадру ≤2 с** — не виміряно, потрібен пристрій і 4G.
- **1–2 виклики малювання на сцену** — не виміряно, потрібен профайлер GPU. Перевірено лише передумову: усе ігрове поле йде одним атласом. Текст HUD — чотири об'єкти `Text` із власними текстурами, тож без растрового шрифта в атласі гейт недосяжний.
- **Арт — заготовки, намальовані кодом.** Кроки [1]–[5] пайплайну плану (генерація, витяг альфи, ручне вирівнювання pivot) не виконані. Справжній арт кладеться в `game/art/src/<ім'я кадру>.png` — код не змінюється.
- **Крос-платформний детермінізм** (iOS проти Android) перевірявся лише всередині одного рушія.
- **Ретеншен D1/D7** — жодного числа, і не буде до софтлончу.
- **Відчуття гри** — 172 автоматичні перевірки логіки й **нуль** перевірок того, чи в неї приємно грати.

---

## 7. Урок, який повторився чотири рази

Тижні 3, 4, 5 і 6 дали ту саму знахідку: **зелений тест доводить лише те, що він перевіряє.**

- Тиждень 3: «зелений» тест на підміну павутини був порожнім — і саме він **ховав** дефект, що павутина декоративна.
- Тиждень 4: чотири дефекти, що підробляли метрики гейта, знайдені **читанням коду після** того, як усі набори стали зелені.
- Тиждень 5: рендер не перевіряв жоден тест взагалі — довелося винести розкладку кадру з Phaser і навчитися малювати її в PNG, щоб просто **побачити**.
- Тиждень 6: `POST /api/event` був відкритий для будь-кого, а продовження списувалися так, що одна покупка давала їх нескінченно. Обидва — самоперевіркою на зелених тестах.

Практичний висновок: після кожної зеленої збірки питати не «чи проходять тести», а **«що саме вони не питають»**.
